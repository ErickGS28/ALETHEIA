# documents-service
NODE_ENV=development

# PostgreSQL — schema 'documents' (schema-per-service)
DATABASE_URL="postgresql://clm_user:clm_pass@localhost:5432/clm_dev?schema=documents"

# Redis (transporte de microservicios)
REDIS_HOST=localhost
REDIS_PORT=6379
